namespace ARPG.Enums
{
    public enum StatModifierType
    {
        /// caution! - the int is used as the order the modifiers are applied 
        Flat = 100,
        PercentAdd = 200,
        PercentMult = 300,
    }
}